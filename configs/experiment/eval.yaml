name: eval_${env.name}
lazy_init_models: True
device: cuda
seed: 42

# Plotting setup
plot_interactions: False
start_from_seen: False
record_video: False
cv_run_idx: all
data_parallel: False
save_subdir: ${now:%Y.%m.%d-%H.%M.%S}

# @package _global_

# Wandb config
project: behavior_transformer_repro
experiment: ${env.name}_${experiment.name}

# Hydra saving 
hydra:
  job:
    chdir: True   
  run:
    dir: ./eval_runs/${experiment.name}/${now:%Y.%m.%d-%H.%M.%S}
  sweep:
    dir: ./train_runs/${experiment.name}/${now:%Y.%m.%d-%H.%M.%S}
    subdir: ${get_only_swept_params:}